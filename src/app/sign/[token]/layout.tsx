export const metadata = {
  title: 'Signature - Stravon',
  description: 'Signez votre intervention',
};

export default function SignatureLayout({ children }: { children: React.ReactNode }) {
  return children;
}
